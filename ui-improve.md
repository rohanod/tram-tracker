Add a vehicle note so that each vehicle(not save entry) can be marked with things. Also the borders are not consistent. init and then also Change it a bit. Redo the layout by keeping the rough layout but reimplementing it. Think of it like compressing it to lose some data but you keep the rough sketch then implementing the "Compressed sketch". You don't need to use the figma but don't go too much away from the original figma design. Also put the stats back in the app like the Figma file says.

Also, does the local storage-data need to be corrected becaseu it doesn't look like it's using all the files. make it clearer so have the filenames for the local cdn be tpg-lines.info.json, tpg-routes.polyline.json, tpg-routes.geojson(if needed, probably not), tpg-stops.compact.json.

data is in storage-data/. use the filenames there and don't rename. remove unnecessary things but ask before removing.
